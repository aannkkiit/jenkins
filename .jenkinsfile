pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "Hello World"'
                sh '''
                    echo "build stage"
                '''
            }
        }
        stage('deploy') {
            steps {
                
                sh '''
                    echo "deploy stage"
                '''
            }
        }
        stage('configchange') {
            steps {
                
                sh '''
                    echo "configchange stage"
                '''
            }
        }
    }
}